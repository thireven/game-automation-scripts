# Code map

**Read this before searching the tree.** It exists so a session can start with
one targeted read instead of a sweep: which file owns what, which document
already answers a question, and where a given name is defined. It is
deliberately short — the depth lives in the documents it points at, and each of
those is worth more than grepping the source it describes.

Paths are relative to `app.gap.Tsum/` unless they are repo-root documents
(`CLAUDE.md` and this file).

`npm run map:check` verifies this file against the tree: a path that moved, a
name family that escaped the file it claims, a source file nobody listed, a
script added without being mentioned. Run it after moving anything.

## Repo shape

```
game-automation-scripts/
├── README.md              what this repo is, and the link to the contributor site
├── CLAUDE.md              rules that override defaults -- read alongside this file
├── CODEMAP.md             this file
├── CONTRIBUTING.md        the short form of the contributor site, for GitHub's
│                          own "contributing" link
├── LICENSE                Apache-2.0, the whole repo
├── NOTICE                 what was inherited from where, and the notices the
│                          release zip carries with it
├── .gitattributes         LF line endings everywhere, PNGs binary -- see CLAUDE.md
├── .editorconfig          stops an editor putting CRLF back
├── .github/workflows/     docs.yml builds website/ and publishes it to GitHub Pages
├── website/               the contributor site (Docusaurus): rewrites of the
│                          documents below for someone new to the tree, with the
│                          code pulled from GitHub at view time and the generated
│                          documents synced in at build time -- website/README.md
└── app.gap.Tsum/          the only package: the Disney Tsum Tsum script
    ├── src/               the script -- concatenated into one bundle, no imports,
    │                      plus tsums.dat, the one file shipped beside it
    ├── tools/             the build and what runs inside it: the host shim, the
    │                      generated docs, the dispatch traces, the checks, releases
    ├── docs/              data the documents are generated from: the observed page
    │                      graph (transitions.json) and the clip library (media.json)
    ├── build/ dist/       build output, git-ignored (also the release zip + .sha256)
    ├── config.json        release identity: game, one entry per channel (name,
    │                      archive, note, and `Status` -- the lowest SkillStatus
    │                      that channel offers), how many versions stay
    │                      installable (version itself: package.json)
    └── build.sh · build.ps1 · debug_deploy.ps1
                           the first two are flag-translating wrappers over
                           tools/build/, which is the build
```

It runs on **Game Automation Platform** and only that — see
[External trees](#external-trees) and `CLAUDE.md` for what that means. The
tooling that *develops* it is the development toolkit, a separate private
repository beside this one — `DEVELOPMENT.md` § The development toolkit.

## Which document answers which question

| Question | Read |
|:--|:--|
| What a change has to touch before it is finished | [§ Adding something](#adding-something--what-a-change-has-to-touch), below |
| How to contribute, explained for someone new to the tree | `CONTRIBUTING.md`, then the site under `website/` (its own commands: `website/README.md`) |
| Under what terms, and what was inherited from where | `LICENSE`, `NOTICE` |
| What the script does; what a setting means | `README.md` |
| The log record schema, the log event vocabulary, reading a run with Logdy | `LOGGING.md` |
| Where the log, the round stats and the screenshots are on the device, and how to get them onto the PC from MuMu or another emulator | `README.md` § Getting logs, stats and screenshots off the device |
| What this script broadcasts to outside tooling, and what each event carries | `EVENTS.md` — generated by `npm run events:docs`, never hand-edit. How those lines leave the device is the host's `../game-automation-app/docs/EVENTS.md` |
| Why the settings page may not use a `<select>` or reload itself | `DEVELOPMENT.md` § The page runs in an overlay |
| Changing a setting without stopping the run; what the Quick Bar is | `README.md` § The Quick Bar, then `DEVELOPMENT.md` § The Quick Bar page |
| Why the Quick Bar and the settings page agree, and what keeps them agreeing | `DEVELOPMENT.md` § Three copies of a setting, and how they stay one |
| How the source fits together, the typing model, building | `DEVELOPMENT.md` — § The one thing to understand first, § How the layers fit together, § Building |
| What one source file owns, in depth | That file's own header comment. [§ Source map](#source-map--src) below is the index into them |
| Symptom → file, for behaviour and for detection | `DEVELOPMENT.md` — § Where to start, § Where to start (page detection) |
| What happens on each recognised screen, in what order | `PAGE_DISPATCH.md` — generated by `npm run pages:docs`, never hand-edit |
| Writing a task that walks a flow: navigating, waiting, tapping a screen that swallows taps, reading a list you are changing | `DRIVING_SCREENS.md` |
| What the router owns and what a flow has to write for itself — and why there is no shared list or grid reader | `DRIVING_SCREENS.md` § What is shared |
| Why navigation is slow, or a wait times out on a screen that is plainly there | `DRIVING_SCREENS.md` §§ 3–4 |
| Why a flow drops taps on one device and not another, or works at 120fps and not 60 | `DRIVING_SCREENS.md` § 5 — animations are frame counts, so wait for the screen to stop moving rather than for a clock |
| Why a screen is not recognised; the offline harness | `DEVELOPMENT.md` § Page detection offline, then `tools/runtime/` |
| Why the same few tsums are drawn at over and over with nothing clearing | `Config.linkReach`'s comment (`src/data.ts`) for the cause, then the dead-scan block above `taskPlayGameQuick` (`src/play.ts`) for what the loop does about it |
| Why a round stopped being played, or the run ended mid-round | The Max Round Duration block above `taskPlayGameQuick` (`src/play.ts`) — `play.roundTimeUp` in the log says which of the three actions ran |
| Which screen actually follows which, and why that is a map rather than a filter | `PAGE_DISPATCH.md` § The sitemap; the ledger behind it is `docs/transitions.json` |
| What the script is about to do next | `src/forecast.ts`'s header |
| How a tsum is identified and named, or why the `tsum` column is empty | `src/roundStats.ts`'s header for the reader; the library it reads, `src/tsums.dat`, is built by the development tools |
| What is *in* an issue report, and how it left the device | `src/report.ts`; the host's `README.md` § Sending in a problem |
| Which tooling is not here, and what the commands comments cite refer to | `DEVELOPMENT.md` § Development tooling outside this repo |
| When something changed, and why | `CHANGELOG.md` — 1.0 on; `CHANGELOG_0.x.md` before that; `CHANGELOG_ELSA.md` for Coronation Day Elsa while she is off the production build |
| How a release is cut, what it publishes, and where | `DEVELOPMENT.md` § Releasing, then `tools/release/` |
| Known bugs, wanted features | `BACKLOG.md` |
| What a host native actually does | `../game-automation-app/CODEMAP.md`, then its `../game-automation-app/docs/API.md` and `app/src/main/cpp/` |

## Source map — `src/`

In bundle order (the `files` list in `tsconfig.json`). One clause per file, so
this stays one read: **the long form is each file's own header comment**, which
is where a decision and its reasoning are kept together. For a name rather than
a file, see [§ Where a name lives](#where-a-name-lives).

| File | Owns | Open it when |
|:--|:--|:--|
| `src/shared.d.ts` | Types only, in **all three** compilations (with `src/logEvents.ts`): `SettingKey`, `Settings`, `SkillType`, `BubbleStrategy`, `RecordKey`, `Locale`. | a setting key, a skill id or a language changes |
| `src/globals.d.ts` | Types only: the host API declarations, the shared value shapes, and `interface Tsum` — every prototype method's signature. | the compiler cannot find a host function, or you added a `Tsum` method |
| `src/settings.d.ts` | Types only, **both** page compilations: the page schema (`SettingSpec`, `GroupSpec`, `TabSpec`, `RowKey`), `Preset`, and `PageMessage` — one page's nudge to the other, a topic carrying no values, so the engine stays the one authority on what a setting is set to. | the pages need a browser-side global, or a new page-to-page message |
| `src/strings.d.ts` | Types only, **both** page compilations: `UiText`, plus `UiStrings` mapped over it so a catalogue and the enum are one list. | adding or removing a piece of page text |
| `src/i18n.ts` | The page-side i18n runtime, **both** page compilations. Resolves at render time, never at load, because the language changes under a page that is already up. | a string comes out in the wrong language, or blank |
| `src/uiEn.ts`, `src/uiZhTw.ts` | One language each, keyed by `UiText`. English is the reference and must be complete; a translation falls back key by key. | wrong or missing page text; `npm run i18n:check` for what a language is missing |
| `src/taskController.ts` | `TsumTaskController`, the cooperative scheduler: which jobs are due, in a total order that never falls to iteration, and running one. Five consecutive throws from one job restart the app. | a task never runs, or runs at the wrong cadence |
| `src/runPlan.ts` | **The run's task table**, in both the game and the settings compilations. `buildRun` registers exactly this and the Run order card lists exactly this, so the two cannot drift. | adding a job, changing who goes first, or the Run order card disagreeing with what runs |
| `src/state.ts` | The two script-wide globals: `ts`, `gTaskController`. | almost never |
| `src/utils.ts` | `isSameColor`, `absColor`, `channelDiff`. | colour comparison |
| `src/data.ts` | **Every coordinate and tuning constant, no logic**: the page fingerprints, the navigation plans, and one table per screen a chore walks. Shared tables only — a table one skill reads lives in that skill's file. | the script taps the wrong place, or a screen needs a fingerprint |
| `src/logEvents.ts` | `Log` — the log event vocabulary, one `const enum` per component inside a namespace erased with them. Every `event` name the project writes, declared once. | adding a log event, or looking for where one is defined |
| `src/scriptEvents.ts` | `Emit` — the **emitted** event vocabulary, the same shape and erased the same way. Separate because these are an interface another program reacts to, not a sentence a person reads: renaming one breaks somebody's tooling. | adding an emitted event |
| `src/logs.ts` | The log sentence **registry**, merging one language over English so a partial translation still writes a full record. No text of its own. | adding a language to the game script |
| `src/logsEn.ts`, `src/logsZhTw.ts` | One language each, keyed by the `Log` constants. English is the reference and must be complete; a translation may omit keys. | wrong or missing user-visible log text |
| `src/logging.ts` | The structured logger: one JSON object per line, the run and round correlation ids, the flood guard, and the **ring** of the last records whether or not they were written out, which is what an issue report carries. `LOGGING.md` is the schema. | adding a log line, or changing what every record carries |
| `src/pathfinding.ts` | Board reading and chain planning, no tapping: circle detection, clustering, the connectivity graph, `calculatePaths`, and `boardStillHolds`. Also the **chroma board colour model**, which is what stops red — it sits on hue's wrap — reading as green, and its **texture axes**, which are what tell two greys apart. Behind the Debug tab, two experiments (`BoardModelConfig`): the **radial** model adds each tsum's rim drop and a k-means pass, and the **fragment merge** folds the small clusters a scan splits off a colour back into it, down to the round's type count; a skill may name its model (`SkillHandler.boardModel`). | chains are short, wrong or slow — the development tools' `chain:bench` says whether the search is the cause; colours that merge or split are the colour model, not the search |
| `src/pages.ts` | `PageRouter` and the `gPages` singleton: score, `observe`, `react`, `detect`, `peek`, `navigate()`, the page sets every narrowed sweep is drawn from, `roundInProgress()` — the stand-aside every chore asks before it navigates — and `perform` — the one place a subscription's `steps` become taps and waits. | how a match is *chosen*, how the queue runs, which pages a narrowed sweep can see, or a chore that ran over a round |
| `src/tsum.ts` | The `Tsum` object itself — the world one run is played in, and how every other file reaches the device: the fields, the geometry `init` works out, the capture and touch wrappers, and the two coordinate conversions. The eight files below reopen this prototype. | a tap lands in the wrong place, the geometry is wrong, or you are adding a `Tsum` method with no better home |
| `src/waits.ts` | The four waits: `sleep`, `sleepUntil`, `settleScreen` (wait for the screen to stop moving rather than for a clock) and `settleBoard` (the play square alone, the only part of the board that ever goes still). `mayContinue()` is here too. | a tap lands inside an animation, or a wait costs more than the thing it waits for |
| `src/appLifecycle.ts` | The game app: whether it is in front, which of its two builds this device plays (`gameBuild`: in front, else last seen, else installed via `pm path` — no setting), getting it there, and the two waits either side of a restart — which watch focus and the first fingerprint rather than a clock. | the game is not running, the wrong build is launched, a restart leaves it somewhere odd, or a tsum is named in the wrong language |
| `src/board.ts` | Reading the board and drawing chains on it: `scanBoardQuick` (one capture, with the bubbles read off that same frame so a pop costs taps and no capture), `link`, and what the Bubble Strategy setting comes to — including the holds over it in a fever's last seconds and for two seconds after a skill activation, and `ripeGameBubbles`, which leaves a bubble that has just appeared, or has no tsums round it, for a later scan. | chains are drawn wrong, bubbles are spent wrong, or the board reads wrong |
| `src/play.ts` | One round, start to finish. Two things make a round hard to end and both live here: proving game over through a burst's animation, and the dead-scan block that notices a chain the game refused. The Max Round Duration cap is the third — the clock giving up on a round that never ends. | a round will not start, ends early, runs forever, or the same few tsums are drawn at with nothing clearing |
| `src/mail.ts` | The mailbox chore. The rows are *found* on the frame rather than measured, so a scrolled list that came to rest between rows still reads. | a gift is missed, a skipped medal or ruby is opened, or the flow sticks on the gift screen |
| `src/hearts.ts` | The send-hearts sweep: rows found wherever the pink is rather than at measured positions, and the end of the list defined as the point a drag stops changing anything. Also the sent/received tally that survives a restart. | hearts are missed, sent twice, or the sweep will not reach the end of the list |
| `src/levelCap.ts` | The level-cap sweep: the sort round trip, the rewind, and one raise at a time off a grid re-read after each, because a raise can move its card out of the order the collection is sorted by. Also Auto Unlock MyTsum Level: the padlock read off the post-round level-up panel's first card, and the one raise after the round that shares the sweep's last three hops. `awaitPage` is shared with `src/boxes.ts`. | a raise is missed, the sweep stops early, it walks the collection in the wrong order, or the MyTsum's cap is read wrong off the level-up screen |
| `src/boxes.ts` | The Box Buying chore, the Pick-Up Capsule included. Stops on the game's own two signals or its own purchase limit, and waits for the store's *contents* rather than for the screen to stop moving — it fingerprints as itself about 1.5s early, holding still the whole time. | the wrong box is bought, nothing is bought, the capsule is read as the Select Box or the reverse, or the reveals will not clear |
| `src/fever.ts` | Fever time: one look with no memory, how long the fever has left (the bar's fill, off a crop read when asked), plus `gFever` — the debounced state and its start/end broadcast. A mode of the board, not a page, which is why it is not in `src/pages.ts`. | anything that has to know a fever is running, or about to end |
| `src/lorcana.ts` | The Lorcana transformation: the medallion reader, the state kept off it with hysteresis each way, and the ink-stone bubbles. A mode of the board like `src/fever.ts`, and one setting rather than a skill — every Lorcana tsum transforms the same way whatever its own skill does. | the card is not tapped, the transformation is called early or late, or the ink stones pile up |
| `src/chainCounter.ts` | **The game's own count of a chain**, read off the number it draws beside the head with the finger still down: the outline of `game_num_48_blue` boxed by connected component, sampled to a grid and scored against `ChainDigits`, templates rendered from the sprite itself by the development tools' `chain:digits`. Logged as `registered` beside `chain` wherever a drag is being measured (Gaston's passes, and the play loop's chains under `readsChainCounter`) — the table a drag setting is judged by. | `registered` is null or wrong on a drag that plainly linked — `chain:replay` reads a recording through the same pipeline |
| `src/skills/` | One file per skill plus the shared core — see [the skills table](#skills--srcskills). | a skill misfires |
| `src/clickAssist.ts` | Click Assist: read the raw Linux touch stream and draw a chain where the user taps. Mutually exclusive with auto-play. | manual-play mode only |
| `src/dialogs.ts` | Android **system** dialogs (root warnings, permissions) — read by panel colour, with a `uiautomator` XML fallback when that is ambiguous — and the `checkStall` progress watchdog. | the script is stuck behind something the game did not draw |
| `src/corpus.ts` | Capture of unrecognised screens on the device, with sidecars, rate-limited per session — the frames the detection suite's corpus is grown from. | the "collect unknown screens" path |
| `src/report.ts` | **The folder a player sends in.** One per issue: the screen, the router's recent frames, a manifest of everything a screenshot cannot carry, and the last few hundred log records — **including the debug ones the log file never gets**. Every part is written under its own guard, because a screen that will not capture is itself a finding. | a report is empty, misses the screens before the moment, or a new failure should write one |
| `src/walkthrough.ts` | The walkthrough recorder. A *mode*: with that setting on it is the only task, and it watches rather than plays — it never calls `detect()`, so no handler fires and nothing taps. Feeds `docs/transitions.json`. | recording the real page graph, or where a button is |
| `src/roundStats.ts` | The per-round CSV, one file per day: template digit reading (the engine has no OCR), the sampling hooks, the read-modify-rewrite the engine's lack of append forces, and the row `id` — minted at the whistle rather than at the write, because it is the **round's** id and every `round.*` event carries it. Also which tsum is selected, read against the library. | a stats column is empty or wrong, the banner names the wrong tsum, or a row never reaches the server |
| `src/tsums.dat` | **Generated, never hand-edited, and not part of the bundle**: the tsum library — one signature, a name per game build and one board colour for each of 797 tsums. Deployed beside the bundle; a missing or stale one empties the CSV's `tsum` column rather than stopping anything, and the two halves are versioned apart in its header. | never — the development tools' lexicon regenerates it |
| `src/forecast.ts` | **What the script is about to do, without doing it** — which subscription will act, which task the scheduler takes next, and the ways off this screen — each answered from what already decides it rather than from a second reading. Read **forward only**. | the `forecast.state` record is wrong, or a handler was added that can decline |
| `src/pageHandlers.ts` | **Every reaction to a page**, one subscription each; reading it top to bottom is reading the dispatch order. Each is a list of `steps` rather than a body, so the budgets are data and `PAGE_DISPATCH.md` states them. | a screen is recognised and nothing happens |
| `src/index.ts` | The entry point and *only* that: `start()` / `stop()` (and `requestStop()`, the flag-raising half a task body may call), the run lifecycle between them, and the globals beyond them that the settings page evaluates by name. Keep it thin — see `DEVELOPMENT.md` § The one thing to understand first for what a fat one cost. | wiring a setting onto `ts`, registering a task, or a stop/restart behaving oddly |
| `src/quickbar.ts` | The engine half of the **Quick Bar** — the strip of live settings the host draws below the game's own lowest button. Three entry points and nothing else. **Waiting for the next round is the default**: a setting the current round was set up under cannot be decided again under a board already dealt. | a Quick Bar control does nothing, or a pause does not pause the round |
| `src/settings.ts` | The settings WebView, compiled separately: the `tabs` schema, template-cloning rendering, debounced persistence, share codes, the Run order card, and the app bar's preset controls. A preset and a settings code carry the same set (`SHARE_SLOTS`) — how a round is played, and nothing else. A row below the channel's floor is not drawn and takes no stored, shared or preset value, but keeps its slot and its default in `start()`. | adding a setting or a task (also edit `src/index.ts`) |
| `src/index.html`, `src/index.css` | The settings page shell: the app bar, the tab bar, and one `<template>` per piece of structure that `src/settings.ts` clones. Pico CSS plus the components it lacks. Both inlined at build time. | settings page layout |
| `src/releaseStatus.ts` | `ReleaseStatus` and the channel floor stamped into it, **both** page compilations: what this build is allowed to offer, asked as `offeredHere`, plus the badge an unfinished thing wears. Read by the skill list and the settings schema alike. | a skill or setting should reach only some channels |
| `src/skillOptions.ts` | The Skill Type dropdown, written once for **both** page compilations: the entries, their three groups, and each one's required `status`. Not in the game bundle. | adding a skill to the dropdown, deciding its group, or which channels should offer it |
| `src/bubbleOptions.ts` | The Bubble Strategy dropdown, written once for **both** page compilations. Each entry carries a `short` as well as a `title`, because the strip's chip is a few characters wide and which words survive that is a translation decision. | adding a bubble strategy, or the strip's chip reading wrong |
| `src/presets.ts` | **Named configurations, and the store both pages read them from.** `presetMatchName` answers *which preset the form currently is* by matching rather than remembering, so nothing has to be invalidated. Held as values rather than as a code, because decoding needs the schema and the strip's compilation has none. | a preset is not found, not saved, or the wrong one is named |
| `src/qrCode.ts` | A QR encoder and nothing else — byte mode, level M, versions 1–6. In the **settings** compilation only. No library, because the page is loaded from `file://` and inlined whole. | the QR will not scan, or the share code outgrows version 6 |
| `src/quickbarPage.ts` | The Quick Bar's page script, a third compilation: **wiring only**. It finds the controls the markup already declared, by their `data-key`, and keeps them in step with the engine. Every change is debounced and written twice, so the strip draws from the tap rather than from the reply. The Report chip is the one control live while the run *goes*: `qbNameHotspot` hands its rect to the host, which keeps that patch touchable. | a Quick Bar control is wired wrong, or Report is dead mid-run; never for layout |
| `src/quickbar.html`, `src/quickbar.css` | **The Quick Bar's whole appearance**, and the file to edit when iterating on it. Every control is written out by hand with a `data-key` and nothing else, so moving or restyling one needs no change to the script. | Quick Bar layout |

## Skills — `src/skills/`

Each file registers itself with `registerSkill({types: [...]})` and is otherwise
a leaf: nothing outside `src/skills/` references its symbols. Adding one means a
new file here, a line in `tsconfig.json` after `skillCore.ts`, a `SkillType`
member in `src/shared.d.ts`, and a dropdown entry in `src/skillOptions.ts` —
under the group its activation belongs to, at its name, with the `status` saying
how finished it is. A new skill is almost always `SkillStatus.Alpha`, which lists
it on Alpha builds only; promoting it later is a one-word edit.

The Note below says only what makes a skill unlike the others — **how one works
is its own file's header comment**, which for the long choreographies runs to a
hundred lines and more.

| File | `SkillType` | Note |
|:--|:--|:--|
| `src/skills/skillCore.ts` | — | The registry and everything shared: gauge read, fever hold-off, activation taps, `useSkill` dispatch, auto-tap, and the declarations a skill makes about the play loop — `sweepsBubbles`, the `claimsBubbles`/`popBubblesAfterChain` pair, `stillRunning` (a full gauge waits while the last activation runs), `chainLimits` (which may be a function of `ts`, for a skill whose board changes shape mid-round), `boardModel` (the colour model its scans cluster with, over the Debug tab's) and `setMyTsumPriority`. Each of those is a **hold** over a setting rather than a write, so the player's own value is untouched and lifting it restores exactly that |
| `src/skills/burst.ts` | `SkillType.Burst`, `SkillType.BurstBubbles` | A bare tap activates, so the play loop may fire these blind between chains |
| `src/skills/pairTsum.ts` | `SkillType.PairTsum` | Two-button activation |
| `src/skills/donald.ts` | `SkillType.Donald`, `SkillType.HolidayDonald` | |
| `src/skills/lukeJ.ts` | `SkillType.JediLuke` | Aimed taps — `lukeJTapTargets` |
| `src/skills/marie.ts` | `SkillType.Marie`, `SkillType.MissBunny`, `SkillType.Rabbit` | |
| `src/skills/moana.ts` | `SkillType.Moana` | |
| `src/skills/mickeyH2015.ts` | `SkillType.HornHatMickey` | |
| `src/skills/snowWhite.ts` | `SkillType.SnowWhite` | |
| `src/skills/cinderella.ts` | `SkillType.Cinderella` | Has its own `useCinderellaSkill` |
| `src/skills/woody2.ts` | `SkillType.SheriffWoody` | |
| `src/skills/cabbageMickey.ts` | `SkillType.CabbageMickey` | Face-colour probe |
| `src/skills/cptLy.ts` | `SkillType.CptLightyear`, `SkillType.CptLightyear120` | Timing tables, one per frame rate |
| `src/skills/lightningMcQueenPlus.ts` | `SkillType.LightningMcQueenPlus` | |
| `src/skills/formalBeast.ts` | `SkillType.FormalBeast` | The only skill that steers the play loop rather than choreographing an activation: it reads the twin gauge his mode draws and picks chains by colour. Nothing is refused — the leading colour is *throttled* to the headroom left under the rose |
| `src/skills/tiaraMinniePlus.ts` | `SkillType.TiaraMinniePlus` | The largest: board signature, cloud sampling, candidate scoring |
| `src/skills/coronationElsa.ts` | `SkillType.CoronationElsa` | The only choreography that *plays*: her activation opens a timed freeze window swept a row at a time -- capture, read the ice, chain the lowest free row, wait for the band -- then one break, because a tsum under two bands counts double |
| `src/skills/coronationElsaLegacy.ts` | `SkillType.CoronationElsaLegacy` | The same window as 1.0 played it, kept beside the rework for side-by-side testing: one capture per pass, every chain planned off a model of what the last one froze, the pile spent as soon as the board is played out. Every symbol suffixed `Legacy`; never tuned -- a finding goes in the file above |
| `src/skills/lorcanaAurora.ts` | `SkillType.LorcanaAurora` | Two skills with one gauge between them, chosen by `gLorcana.transformed` (`src/lorcana.ts`, which plays the transformation). After the card any bubble links to any other at any distance, so the *order* is the thing worth solving, and the bubbles become a standing claim nothing else may spend |
| `src/skills/gaston.ts` | `SkillType.Gaston` | **The longest chain `findLongestTsumPath` finds over the free Gastons, drawn from its higher end, and nothing here consults the chain settings.** Two chains cancelled with a bubble, then one held on its last tsum through the close so its clear charges the next activation, and the button spammed until it fires -- and the window that fires opens from here, window after window (`gastonWindow`), with `stillRunning` refusing the play loop a tap inside one. Which tsums are Gaston is read off the game's own paint with the finger resting on the head (`gastonLinkChain`'s `GastonOracle`: every circle's floor rise, so any theme), the route planned from there over hops that cross no other tsum (`gastonNeighbors`); a head that paints nothing is lifted and another tried, and a pass that cannot read plans off what the last read learned (`gastonCarry`) or the colour cluster -- the cluster again when the carry leaves nothing to chain. Bubbles are the skill's from the first activation on, spent on the leftovers; the route keeps clear of three readings of them (`gastonBubbles`: the Hough pass, a lower-threshold pass over the bowl's bottom, and the round's memory of every read), and a cancel taps the first two, leaving one bubble standing and none spent on a chain under `cancelMinChain`. Gated on the tsum count rather than on stillness. Predicts the backdrop switch from its coming on (read as the chrome not being plain; the antlered backdrop is the skill's own, so the switch is the close) and keeps its drags out of the freeze there; the fever's own start and end, hidden under the antlers, are read off the fever gauge's ring and fill and waited out too (`gastonAwaitFever`). Drawn at `linkTsums`' pace; a chain with no bubble to cancel it is held when its pop would refill past the earliest close (`refillBy`). Each step is checked rather than assumed: a cancel against the tsum count (the remembered bubbles tapped when it did not take), the charge's spam against the held clear's length, and a short chain on a board of leftovers followed by other colours' chains (`mixedChains`) |
| `src/skills/rapunzelPlus.ts` | `SkillType.RapunzelPlus` | The one chain that ignores colour, capped at what the skill level allows. Owns both ends of *drawing* it too: a planning reach below the game's own, its own paced drag, and a light gate that waits out the activation veil rather than scanning under it |

## Tooling map — `tools/`

Everything here runs on the PC, in Node, against the **built** bundle — never on
the device — and everything here is part of the build or a check the build
runs. The development toolkit beyond that is a separate private repository
that runs against this one's build: `DEVELOPMENT.md` § The development toolkit.

| Directory | What it is | Entry points |
|:--|:--|:--|
| `tools/runtime/` | The Node stand-in for the host: `load.js` evaluates the built bundle in a vm, `host.js` shims the ~15 host primitives under it (faithful to the app's C++), `imageio.js` and `matcher.js` are the image work those need, and `transitions.js` reads and writes `docs/transitions.json`, the observed page graph. Every harness — here and in the development tools — goes through this, so the matcher under test is the production one | required by `tools/minify/`, `tools/pageDocs/`, `tools/dispatchEval/`, `tools/liveSettings/` |
| `tools/pageDocs/` | Renders `PAGE_DISPATCH.md` by asking the compiled router for its real dispatch order | `npm run pages:docs`, `pages:docs:check` |
| `tools/eventDocs/` | Renders `EVENTS.md` from the `emitEvent` call sites. The one tool here that reads the **TypeScript program** rather than the built bundle, because what matters about an emit is what a bundle throws away: its file and line, and each payload field's type | `npm run events:docs`, `events:docs:check` |
| `tools/dispatchEval/` | **Whether a change to one page changed what happens on another.** Golden traces for the dispatch queue and the scheduler, off the built bundle with no device, so a change for page A that moved page B's trace fails before it ships. `tools/dispatchEval/README.md` is the long form | `npm run dispatch:eval`, `dispatch:update` |
| `tools/codemap/` | Checks this file against the tree | `npm run map:check` |
| `tools/i18n/` | What each language is missing, which `UiText` keys nothing names any more, and — the one thing it fails on — a `data-i18n` in the markup naming no key. The rest the compiler already catches | `npm run i18n:check` |
| `tools/liveSettings/` | **When each setting reaches a run in progress**, checked by running the built bundle in a vm rather than by reading it: that every `SHARE_SLOTS` row has an answer, that the switch and the reported state match it, and that the value survives the round trip. It cannot decide *which* kind a setting is — that judgement is what the table records | `npm run live:check` |
| `tools/release/` | Cuts a release: builds the channel, names the archive from `config.json` and `package.json`, writes it and a running per-channel `CHANGELOG.md` into the catalogue, and prunes past `HistoryLimit`. The release note is not taken on trust — `review.js` shows it as the app will render it and waits to be told | `npm run release:alpha`, `release:beta`, `release:production` |
| `tools/build/` | **The build, as a dependency graph.** Each step declares what it `needs` and anything whose needs are met runs, up to one job per core — ~16s of sequential steps down to ~4s, the length of the longest chain. The doc and check steps are `optional`: they print findings and never block a build. `build.sh` and `build.ps1` translate flags into this and do nothing else | `npm run build`, `buildAndAdb`; `--channel`, `--adb`, `--device`, `--jobs` |
| `tools/inline/` | Folds `pico.css`, `index.css` and `settings.js` into one `dist/index.html`, so the page needs no network | run by `tools/build/` |
| `tools/minify/` | terser over both built scripts, **whitespace only** — `compress` and `mangle` are off, so nothing is renamed or rewritten, and it evaluates what it produced. Also copies `src/tsums.dat` into `dist/` without its `#` header | run by `tools/build/` |

`tools/codemap/symbols.js` is the general-purpose symbol extractor (TypeScript
parser; name → file, line, kind) — reuse it rather than writing another one.

## Where a name lives

Naming is consistent enough that a prefix usually settles it, and `map:check`
enforces every row below, so these are true rather than approximate.

| Name | Defined in |
|:--|:--|
| `Tsum#*` — any `ts.foo()` / `this.foo()` method | `src/tsum.ts`, `src/waits.ts`, `src/appLifecycle.ts`, `src/board.ts`, `src/play.ts`, `src/mail.ts`, `src/hearts.ts`, `src/levelCap.ts`, `src/boxes.ts`, `src/dialogs.ts`, `src/roundStats.ts`, `src/corpus.ts`, `src/report.ts`, `src/clickAssist.ts`, `src/walkthrough.ts`, `src/fever.ts`, `src/skills/skillCore.ts`, `src/skills/tiaraMinniePlus.ts`, `src/skills/cinderella.ts`, `src/skills/cptLy.ts`, `src/skills/formalBeast.ts`, `src/skills/coronationElsa.ts`, `src/skills/coronationElsaLegacy.ts`, `src/lorcana.ts` — every signature also in `src/globals.d.ts` |
| `Tsum#task*` — the scheduled jobs | `src/play.ts`, `src/mail.ts`, `src/hearts.ts`, `src/levelCap.ts`, `src/boxes.ts`, `src/appLifecycle.ts`, `src/clickAssist.ts`, `src/walkthrough.ts` |
| `TaskName`, `JobPriority`, `TaskSpec`, `RunSettings`, `runTaskTable` — the task table | `src/runPlan.ts` |
| `ScriptVersion`, `Config`, `Button`, `Page`, `PageName`, `PageProfiles`, `NavPlans`, `PageAnchor`, `RouteSource`, `AnchorRoutes`, `PageRoutes` | `src/data.ts` |
| `forecast*`, `Forecast*` | `src/forecast.ts`, `src/globals.d.ts` |
| `PageRouter#*`, `PageKind`, `PageRole`, `PageCategory*`, `subscriptionTakes`, `gPages` | `src/pages.ts` |
| `PageDef`, `PageMatch`, `PageMap`, `PageProfile`, `PageProfileMap`, `PageSubscription`, `PageSubscriptionEntry`, `PageVisit`, `PageEvent`, `PageColor`, `PageRoute`, `PageRouteMap` | `src/globals.d.ts` |
| `Fever*`, `gFever` | `src/fever.ts`, `src/data.ts` (`FeverProbes`, `FeverBar`), `src/globals.d.ts` |
| `skill*`, `Skill*` | `src/skills/skillCore.ts`, `src/tsum.ts`, `src/globals.d.ts`, `src/shared.d.ts`, `src/skillOptions.ts` |
| `quickBar*`, `onPause` | `src/quickbar.ts` |
| `qb*`, `QB_*`, `onQuickBar*`, `onGapState` | `src/quickbarPage.ts` |
| `*LiveSettings`, `onLiveSettingsApplied`, `LIVE_POLL_MS` — the settings page and the running world, kept in step | `src/index.ts` (`applyLiveSettings`, the engine's end), `src/settings.ts` (`pushLiveSettings`, `pullLiveSettings`, `onLiveSettings`, and `onLiveSettingsApplied` — the engine confirming a push, which is where the nudge to the other page goes out) |
| `StorageKey`, `PageMessage` | `src/settings.d.ts` |
| `preset*`, `Preset*`, `PRESET_*` — the named-configuration store and the page halves that drive it | `src/presets.ts` (the store), `src/settings.ts` (the app bar's dropdown, the save panel, the export row), `src/settings.d.ts` (`interface Preset`) — the Quick Bar's own half is `qb*`, in `src/quickbarPage.ts` |
| `onGapMessage` — the page-to-page nudge's handler, one per page | `src/settings.ts`, `src/quickbarPage.ts` |
| `tiara*`, `Tiara*` | `src/skills/tiaraMinniePlus.ts` (tables included), `src/globals.d.ts` |
| `formalBeast*`, `FormalBeast*` | `src/skills/formalBeast.ts` (tables included), `src/globals.d.ts` |
| `elsa*`, `CoronationElsa*` | `src/skills/coronationElsa.ts` (tables included), `src/skills/coronationElsaLegacy.ts` (the `elsaLegacy*` / `CoronationElsaLegacy*` half, and only that), `src/globals.d.ts` |
| `lorcana*`, `Lorcana*`, `gLorcana` | `src/lorcana.ts` (tables included), `src/skills/lorcanaAurora.ts`, `src/globals.d.ts`, `src/shared.d.ts` (`SkillType.LorcanaAurora`, `SettingKey.LorcanaCard`), `src/tsum.ts` (the `lorcanaCard` field) |
| `rapunzel*`, `RapunzelPlus*` | `src/skills/rapunzelPlus.ts` (tables included) — plain functions, no `Tsum` method |
| `gaston*`, `Gaston*` | `src/skills/gaston.ts` (tables included) — plain functions, no `Tsum` method |
| `chain*`, `Chain*` — the chain counter's reader, `ChainDigits` (generated) and `ChainCount` | `src/chainCounter.ts` — plain functions, no `Tsum` method; `readsChainCounter` is the `SkillHandler` flag in `src/skills/skillCore.ts` |
| `stats*`, `Stats*` | `src/roundStats.ts`, `src/globals.d.ts` |
| `TallyRow`, `tallyRow`, `readTallyRow`, `tallySkip*`, `tallyCountingUp` — the score tally's button row, read once a look and shared by the count-up tap, the Play shortcut and the stats read | `src/globals.d.ts` (the interface), `src/roundStats.ts` (`readTallyRow`, the three probes' readers), `src/tsum.ts` (the fields), `src/pageHandlers.ts` (`record.tallyRow`, `dismiss.tallyCountUp`, `nav.move.tallyToGame`) |
| `maxRound*`, `MaxRound*`, `RoundCoast*`, `RoundLook`, `HudWatch` — the Max Round Duration cap and the liveness look it shares with the play loop | `src/play.ts` (the cap block and `watchRoundEnd`), `src/shared.d.ts` (`MaxRoundAction`, and the two `SettingKey`s), `src/tsum.ts` (`maxRoundMs`, `maxRoundAction`), `src/globals.d.ts` (`watchRoundEnd`'s signature), `src/quickbar.ts` (both `LiveWhen.Now`), `src/settings.ts` (the pair of Gameplay rows, `neverShared`) |
| `Box*` — the Box Buying chore's vocabulary | `src/shared.d.ts` (`BoxType`, the five boxes a player may pick, Select Box and the Pick-Up Capsule sharing the limited-time slot; `BoxPurchaseSize`, one, ten, or ten then one), `src/data.ts` (`BoxStore`, the store's geometry, with `capsuleIcon` telling the capsule's tab from the Select Box's; `BoxPurchaseResultPage`, the tally's fingerprint; `BoxTenTimeRefused`, the targeted toast the 10-Time button raises on a nearly empty box), `src/globals.d.ts` (`BoxButtonState`, `BoxTabRow`, `BoxPurchaseOffer`, `BoxDialog`, `BoxPurchaseOutcome`), `src/boxes.ts` (`BoxReveals`, and the `BuyBox*` tuning constants) |
| `buyBox*`, `readBox*`, `awaitBox*`, `openBoxTab`, `clearBoxReveals` | `src/boxes.ts` (the flow), `src/tsum.ts` (`buyBoxType`, `buyBoxSize`, `buyBoxMaxPurchases` — the settings fields, on the class with the rest), `src/globals.d.ts` (its signatures), `src/index.ts` (`buyBoxesNow`, `queueBuyBoxSweep`), `src/settings.ts` (`askBuyBoxesNow`) |
| `myTsum*`, `MyTsum*` | `src/roundStats.ts`, `src/data.ts` (`MyTsumPortrait` — both rects, the grid, the mask, the thresholds; `MyTsumBoard` — how a tsum's board colour was derived, and how near a cluster must be), `src/globals.d.ts`, `src/tsum.ts` (the `myTsum` field, and the `myTsumCap*` pair the level-up read leaves for the play task — `LevelUpMyTsumCard` in `src/data.ts` is what it reads) |
| `GameBuild`, `GamePackages`, `focusedPackage`, `installedGameBuilds`, `Tsum#gameBuild` — the two game packages, which is in front, which is installed, and the one this device plays | `src/globals.d.ts` (the enum), `src/appLifecycle.ts` (the reads; the focus one is shared with `isAppOn`) — launched and force-stopped there, named by in `selectedTsum` (`src/roundStats.ts`), and written as the stats CSV's `build` column, the report manifest's `script.build` and the corpus sidecar's `build` |
| `detectMyTsum`, `DetectMyTsum*` — the Debug tab's Detect button: the pre-round tsum read on demand, no round needed | `src/roundStats.ts` (`detectMyTsum`, the page entry point — refuses a live run, builds a throwaway `Tsum`, waits for the panel to leave the frame), `src/shared.d.ts` (`DetectMyTsumRefusal`, the reasons that cross the bridge), `src/settings.ts` (`askDetectMyTsum`, `onMyTsumDetected`, the status line under the row) |
| `dialog*`, `Dialog*`, `Stall*` | `src/dialogs.ts`, `src/globals.d.ts` |
| `corpus*`, `Corpus*` | `src/corpus.ts` |
| `report*`, `Report*` — the issue-report folder and what triggers one | `src/report.ts`, `src/settings.ts` (the Debug tab's Report row and its note panel), `src/quickbarPage.ts` (the strip's chip). `Config.reportTrailFrames` is the exception and lives in `src/data.ts`, because the router reads it when its singleton is built |
| `logRing*`, `gLogRing*`, `LogRingMax` — the records a report carries | `src/logging.ts` |
| `walk*`, `Walk*` | `src/walkthrough.ts`, `src/globals.d.ts` |
| `find*` | `src/pathfinding.ts`, `src/dialogs.ts`, `src/clickAssist.ts`, `src/globals.d.ts` |
| `Log` — the log event vocabulary namespace | `src/logEvents.ts` |
| `Emit` — the emitted event vocabulary namespace | `src/scriptEvents.ts` |
| `emitScriptEvent` — the one `typeof emitEvent` guard, which `Tsum#emit` and `src/index.ts` both go through | `src/tsum.ts` |
| `declareReadTop` — the one caller of the host's `setReadTop`: the top-most row the tables read, handed over so the status line stays above it | `src/tsum.ts`, `src/globals.d.ts` |
| `Logs*` — one table per language | `src/logsEn.ts`, `src/logsZhTw.ts` |
| `LogCatalogue`, `LogCataloguePartial`, `logRegisterStrings`, `logStringsFor`, `gLogCatalogues` | `src/logs.ts` |
| `i18n*`, `gLocales`, `gLocaleFallback` | `src/i18n.ts` |
| `UiText`, `UiStrings`, `UiStringsPartial`, `LocaleEntry` | `src/strings.d.ts` |
| `logDebug`, `logBeginRun`, `logEndRun`, `logBeginRound`, `logSetMessages`, `logEmit`, `logDispatch`, `logMessageFor`, `logComponentOf`, `logAddRunContext`, `logAllow`, `LogLevel`, `LogEvent`, `LogFields`, `LogMinSpacingMs`, `LogBurst`, `gLogRunId`, `gLogRoundId`, `gLogMessages`, `gLogAllowance`, `gLogLastAt`, `gLogDropped` | `src/logging.ts` |
| `logInfo`, `logWarn`, `logError` | `src/logging.ts`, `src/settings.ts` — the settings page is a separate compilation and carries its own small implementation of the same record schema (`settingsLog`) |
| `SHARE_*`, `share*` | `src/settings.ts` — `drawShareQr` and `SHARE_QR_WIDTH` included: the panel's drawing is the page's business, the encoding is not |
| `qr*`, `QR_*`, `Qr*` | `src/qrCode.ts` |
| `tabs`, `TabSpec`, `GroupSpec`, `SettingSpec`, `RowKey` | `src/settings.ts`, `src/settings.d.ts` |
| `SettingKey`, `Settings`, `RecordKey`, `Locale` | `src/shared.d.ts` |

Host natives — tap, getScreenshot, execute, inRange, houghCircles and the rest —
are only *declared* in `src/globals.d.ts`; they are implemented in the host app,
under `../game-automation-app/app/src/main/cpp/`. A `Tsum` method of the same
name (`ts.tap`,
`ts.getColor`) is the wrapper around one, in `src/tsum.ts`.

One more that is not where it sounds like it is: page subscriptions live in
`src/pageHandlers.ts` and never in `src/pages.ts`.

A skill's tuning tables live in its own skill file, under a "Tuning data"
heading below the file header — not in `src/data.ts`, which keeps only what
more than one file reads.

## Before editing anything

The rules that are silent when broken. `DEVELOPMENT.md` explains each.

1. **No imports, no modules.** Every `src/*.ts` is concatenated into one
   `build/index.js` and shares one global scope. Order in `tsconfig.json`
   matters for load-time work; duplicate names silently overwrite.
2. **Adding a `Tsum` method means adding its signature to `interface Tsum`** in
   `src/globals.d.ts`. The class is reopened from ten files by declaration
   merging.
3. **Three compilations.** The game bundle (`tsconfig.json`, strict, ES2023),
   the settings UI (`tsconfig.settings.json`, ES5, loose) and the Quick Bar page
   (`tsconfig.quickbar.json`, ES5, strict). The bundle shares only
   `src/shared.d.ts` and the `start({...})` string with the other two; the two
   pages also share `src/settings.d.ts`, `src/strings.d.ts`, `src/i18n.ts`, the
   `ui` catalogues and `src/skillOptions.ts`. Only the first
   has a name an editor discovers, so `settings.ts` carries `/// <reference>`
   lines to keep one from checking it against nothing -- `npm run typecheck` runs
   all three.
4. **A setting is named by `SettingKey`, never by a literal**: add the member in
   `src/shared.d.ts` — `interface Settings` is keyed from it — then the row in
   `src/settings.ts` and the read in `src/index.ts`. Misspelling one is now a
   build error; *forgetting* the read in `index.ts` still is not.
5. **String vocabularies are `const enum`s** — `PageName`, `SkillType`,
   `SettingKey`, `RecordKey`, `SkillReadiness`, `KeyCode`. Use the member, not
   the literal.
6. **`PAGE_DISPATCH.md` and `EVENTS.md` are generated.** Change
   `src/pageHandlers.ts` or an `emitEvent` call and rebuild; editing either
   document by hand is lost at the next build.
7. **Some oddities are deliberate.** This package was forked from a Robotmon
   script and its behaviour was held identical to that original for a long time,
   so a value or an ordering that looks arbitrary may be load-bearing and
   uncommented. Prefer a measurement to a tidy-up — `DRIVING_SCREENS.md` § 5 is
   the worked example.
8. **The version lives in `package.json` and nowhere else.** The settings page
   carries a `$VERSION` placeholder, substituted at build time beside
   `$BUILD_DATE`, and so does the game bundle (`ScriptVersion`, `src/data.ts`,
   stamped on the way into `dist/` so the round-stats CSV can say which build
   played a round); `tools/release/config.js` `loadVersion()` is the reader.
9. **Robotmon is gone on purpose** (ES2023, quality-100 captures, no
   `BOOTCLASSPATH`, no guards around optional natives) — `CLAUDE.md`.
10. **The settings page is a WebView in an overlay window**, so it gets no
   native popups (a `<select>` renders, then does nothing at all when tapped)
   and no `location.reload()` (it navigates to the script directory and blanks
   the page). Redraw with `renderPage()` instead — `DEVELOPMENT.md` § The page
   runs in an overlay.
11. **A log line is one JSON record, not a sentence.** `console.log` is the
   logger's own business; everything else goes through `logInfo` and friends,
   and values go in *fields* rather than concatenated into the message. The top
   level is a fixed envelope and everything a call site says lands in `data` —
   so nothing new belongs at the top level unless it would be true of a log line
   in any program. One plain-text line is all it takes to break a JSONL reader on
   the far end, which is why the settings page and the host app match.
   `LOGGING.md`.
12. **Every timestamp is UTC.** `Date.now()` is epoch milliseconds, log records
   are `toISOString()`, and the stats CSV writes `YYYY-MM-DD HH:MM:SS` off the
   `getUTC*` getters -- file names, day buckets and debug-shot stamps included.
   A local-time reading (`getHours`, `toLocaleString`, a bare `new Date(y, m, d)`)
   makes files from two devices unmergeable, which is the whole point of the
   `id` and `datetime` columns. The PC tooling follows the same rule.

## Adding something — what a change has to touch

The tables above say where things *are*; this one says what is not finished
until it is done, in order. Almost every row fails **silently**: a file the
bundle never loads, a method the compiler cannot see, a screen no frame ever
tests, a generated document that is now a lie. `DEVELOPMENT.md` § Where to start
is the same knowledge indexed by symptom instead. Where "Then run" names a
`pages:*` command, that is the development toolkit (`DEVELOPMENT.md` § The
development toolkit), run against this repo's build.

| Adding | Touch, in this order | Then run |
|:--|:--|:--|
| a fingerprint for a screen nothing recognises | a frame of it in the development tools' corpus, authored into probes in their studio → an entry in `Page`, `src/data.ts` → its `roles` in `PageProfiles` when a flow's narrowed sweep has to see it: a screen a round can end on, one the heart sweep branches on, an interruption → a `PageRoutes` row with `via: PageAnchor.Back` if its `back` is a way out and navigation may have to leave it; without one nothing taps it blind, and `navigate()` reports `nav.noRoute` | `pages:eval`, `pages:calibrate`, `pages:audit`, `dispatch:eval` |
| another **configuration** of a screen already recognised — one page drawn two ways | the frame's `variant` in its corpus sidecar → a second `Page` entry under the same `name` carrying that `variant` | `pages:eval` — its CONFIGURATIONS section is the gate |
| a reaction to a screen | one `gPages.subscribe` in `src/pageHandlers.ts`, in the right band → `pages`, always: the names, or a set off the tables (`allPages`, `pagesOfKind`, `pagesWithAnchor`, `src/pages.ts`) → `acts` if it can decline, which the dispatch tests for it → `steps`, and the edge in `PageRoutes` (`src/data.ts`) if what its first tap presses is not declared yet | `pages:docs`, `pages:selftest`, `dispatch:update` and read the diff |
| a **mode** the board is in rather than a screen — fever time is the worked example, Formal Beast's gauge the second one | a probe table in `src/data.ts` → a watcher modelled on `src/fever.ts` → whatever feeds it readings: a `record` subscription in `src/pageHandlers.ts` when everything has to know, the one caller when only one does (`SkillHandler.orderPaths`, `src/skills/formalBeast.ts`) → types in `src/globals.d.ts` | `pages:eval`, then drive the built bundle under the `tools/runtime/` shim |
| a `Tsum` method | the `Tsum.prototype.x = ...` in whichever file owns the behaviour → its signature in `interface Tsum`, `src/globals.d.ts` | `typecheck` |
| a **new `src/` file** | `tsconfig.json`'s `files` (order matters) → a row in the source map above → a row in *Where a name lives* if it owns a name family → `LexicalGlobals` in `tools/runtime/load.js` if it declares a `class`, and `wanted` in `tools/minify/minify.js` if it declares a singleton reached by name | `map:check`, `build` |
| a setting | `src/shared.d.ts` — a `SettingKey` member, then its field in `Settings` → a row in `src/settings.ts` (`tabs`) → a slot on the end of `SHARE_SLOTS` if the row is on the Gameplay or Skills tab *and* is about how a round is played, which is what puts it in share codes and presets; `neverShared: true` if it is on one of those tabs but shapes the run instead (Auto Play Game, the between-rounds delay, Track round statistics, the Max Round Duration pair); nothing either way if it is on neither tab → the read in `src/index.ts` (`buildRun`) → **an entry in `LiveSettings`** (`src/quickbar.ts`) saying when it reaches a run already going. `nextRound` is the default and needs no argument; `now` is the one to justify, and the entry has to name where the play loop re-reads the setting. A slotted row with no entry at all fails `live:check`, which is the guard against the oldest bug in this area — a setting that silently does nothing until the script is restarted → `status: ReleaseStatus.Alpha` while it is unfinished, plus a matching `data-status` if it also has a Quick Bar cell | `typecheck`, `live:check`, then open the page once and read the log — `checkShareSlots` is what says the slot was forgotten |
| a setting to the **Quick Bar** | a cell in `src/quickbar.html` carrying its `SettingKey` as `data-key` → a `case` in `quickBarApply` and a line in `quickBarState`, both `src/quickbar.ts`. Nothing in `src/quickbarPage.ts` unless it is a *dropdown*, which needs its option list in `qbOptionsFor` — the lists themselves are shared files (`skillOptions.ts`, `bubbleOptions.ts`) so the panel and the strip cannot drift. Only settings that can be changed *mid-run* belong there — anything that decides the task set (`autoPlayGame`, `clickAssist`) needs a fresh `start()`. One that reshapes a round already dealt is `nextRound` in `LiveSettings`, so the whistle applies it instead. Anything that is *not one setting* is the exception and does need page code, because `quickBarApply` carries one value at a time — `.qb-preset` is the one, and it calls `applyLiveSettings` with a whole form | `typecheck`, `live:check` |
| a scheduled task | a `TaskName` member and a row in `runTaskTable`, `src/runPlan.ts` → its `case` in `taskBody`, `src/index.ts` (the method it runs) → its `case` in `taskLabel` and `taskDetail`, `src/settings.ts` (what the Run order card calls it). The two switches are exhaustive, so a name without a body or a label is a build error → a preset in `tools/dispatchEval/presets/` if the job changes who goes first | `typecheck`, `dispatch:update` and read the scheduler diff |
| a skill | the [skills table](#skills--srcskills) — a file, a `tsconfig.json` line after `skillCore.ts`, a dropdown entry carrying the `status` that decides which channels offer it | `typecheck` |
| a tsum to the library | nothing in this repo by hand: the development tools' lexicon finds what `src/tsums.dat` is missing from the game's own art, asks for the printed name, and rewrites the file here. A sprite with no name strip still enters the library under its shorthand — being able to tell a tsum apart and being able to print its name are separate facts | the lexicon's own `--verify`, then `build` — the library ships beside the bundle |
| a user-visible log message | `src/logsEn.ts`, keyed by the event's `Log` constant → the same key in `src/logsZhTw.ts` and any other language, or it falls back to English | `typecheck`, `i18n:check` |
| a piece of **page** text | a `UiText` member in `src/strings.d.ts` → English in `src/uiEn.ts` (required — the build fails without it) → the same key in each other language, or it falls back. Never a literal in `settings.ts` or the markup | `typecheck`, `i18n:check` |
| a **language** | a `Locale` member in `src/shared.d.ts` → a page catalogue beside `src/uiEn.ts` and a log one beside `src/logsEn.ts`, each registering itself → a line in the two page tsconfigs, in `tsconfig.json` and in both build scripts → a `<script>` tag in `src/index.html` and `src/quickbar.html`. The picker, the fallback and the `start()` payload need no edit at all | `typecheck`, `i18n:check` |
| a log line | pick an event name `component.thing.happened` → a member in `src/logEvents.ts`, in its component's enum → `logInfo` / `logWarn` / `logError` / `logDebug` with **fields, never a sentence with numbers spliced in** → a catalogue entry in `src/logsEn.ts` if it is user-visible. `LOGGING.md` § Writing a record | `typecheck` |
| a failure that should collect **an issue report** on its own | one entry in `ReportTriggers` (`src/report.ts`), keyed by the `Log` constant the failure already writes — and nothing at the call site, because the logger is what checks the list. It has to be an event that is genuinely rare: the interval and the per-run cap are a backstop, not the design | `live:check`, then read a report the trigger wrote |
| an **emitted** event | a member in `src/scriptEvents.ts`, with a line of JSDoc — `EVENTS.md` prints it → `this.emit(Emit.X.Y, {…})`, or `emitScriptEvent` where there is no `ts` → nothing else: `EVENTS.md` is generated from the call site. The payload is the interface, so emit the same shape from every site and treat a field's removal as a breaking change | `events:docs`, `typecheck` |
| a change to how a **score or coin figure is read** — a rectangle, a cutoff, a digit template, the glyph pipeline in `src/roundStats.ts` | the change itself, then the frame that motivated it filed in the development tools' corpus with its numbers labelled — the device saves one as `tsum_record/unread-<fields>-….png` for every field it could not read | `pages:stats`: every labelled field reads, every labelled glyph clears both floors, every digit has a sample at every size — a change that moves a digit under a floor or leaves one unmeasured fails here rather than on the device |

Four rules the tables cannot carry:

- **Which band a subscription joins is a contract, not a preference.**
  `observe` promises to take no captures and tap nothing; `record` is for
  measurements that need an untouched screen, captures included; `guard`,
  `dismiss` and `navigate` may tap; `notify` decides nothing. A handler that
  captures belongs in `record` — which is where the fever reading is, and why.
  Nothing checks this, and `PAGE_DISPATCH.md` will happily document the wrong
  choice.
- **Bubble taps go through the Bubble Strategy setting, or they are an
  override that says so.** Bubbles are worth more popped inside a chain than
  popped alone, so the play loop hoards them: `popGameBubbles` spends only what
  `bubbleTapBudget` allows, and the periodic blind sweep runs under one strategy
  only — and not even then while a skill's claim stands, since a blind sweep
  cannot respect one. Two deliberate exceptions, and both say so in `registerSkill`:
  `sweepsBubbles` + `clearAllBubbles`, for skills that turn tsums *into* bubbles
  and so have no chain to save them for; and `claimsBubbles`, for a skill that
  makes the chain itself, where the strategy's own pop is a link taken out of it
  (`bubbleTapBudget` returns 0 while the claim stands, and what the skill still
  wants spent it asks for per chain through `popBubblesAfterChain`). A new tap on a
  bubble that reads none of these silently undoes the setting.
- **A mode of a screen is not a page, and must not be read as one.** `variant`
  is documentation and tooling *by contract* (`PageDef` in `src/globals.d.ts`) —
  the script only ever learns the page name — so state like "a fever is running"
  is read from its own probe table (`ts.isFeverTime`), never from the matched
  key. Declaring the entry anyway is usually worth it: it gives the corpus
  configuration something to be tested against. Make it the plain entry's probes
  *plus* the new ones, because `betterPageMatch` ranks on probe count first and a
  shorter entry can never win.
  **Usually, not always** — a mode too small to survive the capture path cannot
  have an entry at all, and must not be given a `variant` either, because a
  configuration nothing fingerprints reads as a missing entry for ever.
  Formal Beast's twin gauge is the case: its channel is ~20 logical px thick,
  three pixels on the 360px-wide capture page probes are read from, so it is
  read at native resolution off a crop instead (`Tsum.readFormalBeastGauges`) and
  its three corpus frames are plain `GamePlaying`. The rule for deciding is the
  studio's probe sweep: if nothing usable separates the mode from the page's
  other frames, there is no entry to write.
- **Detection claims are cheap to prove, so prove them.** The detection suite
  runs production detection over its corpus, and against `dist/index.js` over
  what actually ships. Anything beyond page matching — a watcher, a state
  machine, a new `Tsum` method — can be driven the same way: `createRuntime` and
  `createTsum` from `tools/runtime/load.js` give you the real bundle over a real
  frame in a few lines.

Finish with `map:check`, `pages:docs` (or a build), and a `CHANGELOG.md` entry
under the section for the version
in `package.json` — plus a one-line `### Summary` bullet there if a player would
notice the change.

## Commands

Run from `app.gap.Tsum/`.

| Command | Does |
|:--|:--|
| `npm run typecheck` | All three compilations. `npm run typecheck:game`, `npm run typecheck:settings` or `npm run typecheck:quickbar` for one |
| `npm run build` | `tsc` both halves, regenerate `PAGE_DISPATCH.md`, inline the settings page → `dist/` (substituting `$BUILD_DATE`, `$VERSION` and `$SKILL_STATUS` — the channel's skill floor, `src/skillOptions.ts` — into it, and `$VERSION` into the game bundle on its way to `dist/`), then the channel's archive and its `.sha256` beside it (the bare digest, as a string). The archive is named from `config.json` (channel) and `package.json` (version), so `-c Beta` / `-Channel Beta` picks which. `npm run build:ps` is the PowerShell equivalent |
| `npm run buildAndAdb` | Build, then push to the device. `npm run adb` pushes an existing `dist/` |
| `npm run pages:docs` | Regenerate `PAGE_DISPATCH.md` (also run by the build); `npm run pages:docs:check` is the gate that fails |
| `npm run events:docs` | Regenerate `EVENTS.md` from the `emitEvent` call sites (also run by the build); `npm run events:docs:check` is the gate that fails. Also reports a name outside `Emit`, one not known at compile time, and an event emitted with two payload shapes |
| `npm run dispatch:eval` | The dispatch queue and the scheduler against their golden traces (also run, non-fatally, by the build): exit 1 on a changed row or a broken invariant. `-- -v` lists every changed row, `-- --only X` narrows to rows or presets containing X, `-- --dispatch` / `--scheduler` runs one half, `-- --strict` makes every invariant a gate |
| `npm run dispatch:update` | Rewrite the goldens from the bundle as it stands. The diff it produces is the review: a row that moved for a page the change was not about is the failure this tool exists for |
| `npm run map:check` | Verify `CODEMAP.md` against the tree |
| `npm run i18n:check` | What each language is missing, and a `data-i18n` naming no key |
| `npm run live:check` | Every `SHARE_SLOTS` row has a `LiveSettings` answer, and the value survives the round trip (also run by the build) |
| `npm run release:alpha` | Build the Alpha channel and publish it to the catalogue. `npm run release:beta` and `npm run release:production` are the other two. `-- --dry-run` shows the entry without writing it |

The contributor site is its own package with its own scripts, run from
`website/` and listed in `website/README.md`.

## External trees

| Tree | Path | Relationship |
|:--|:--|:--|
| **Game Automation Platform** | `../game-automation-app` | The host app these scripts run on, and the only target — it declares the natives, runs the bundle, and installs it. Editing it is allowed (`CLAUDE.md`); see [the host app at a glance](#the-host-app-at-a-glance) below |
| **game-automation-catalogue** | `../../game-automation-catalogue` | Where a release is published: the release commands write the archive and its metadata file into `Official/LineTsumTsum/Alpha`, `.../Beta` or `.../Production`, and the catalogue's own build-official script folds them into its index. The last few archives stay there so the app can offer them as older versions. Only the release tool writes here |

### The host app at a glance

<!-- map:external -->

Enough to know where to look when a question is really about the host rather
than the script. Paths are inside `../game-automation-app`.

| Where | What |
|:--|:--|
| `docs/API.md` | The natives as a script sees them — the reference `src/globals.d.ts` is typed against |
| `app/src/main/cpp/api_image.cpp` | Capture, crop, resize, colour reads, template matching, the OpenCV helpers. The semantics `tools/runtime/host.js` shims |
| `app/src/main/cpp/api_device.cpp`, `api_system.cpp`, `api_common.cpp` | Taps and swipes; shell, files, packages; the shared plumbing |
| `app/src/main/cpp/engine.cpp`, `host.cpp` | The QuickJS-ng engine and the bridge that publishes the natives into it |
| `app/src/main/java/com/gameautomation/platform/` | The Android side: `service/` (the running script), `script/` (loading from `sdcard/GameAutomationPlatform/scripts/`), `ui/`, `device/`, `engine/` |
| `tools/gap-cli.js` | Drives a device from the PC: install, run, screenshot |
| `CODEMAP.md` | The host's own code map — the equivalent of this file, and the fastest way into that tree |
| `samples/` | A minimal example script: `index.js` plus an HTML/CSS settings page |

## Keeping this file true

`npm run map:check` (also run, non-fatally, by the build) checks that:

- every path mentioned here exists;
- every `src/` file, `tools/` directory and Markdown document is mentioned here;
- every name family above really is confined to the files it names, and matches
  at least one symbol;
- every `SkillType` in the skills table is registered in the file beside it;
- the command table and `package.json` scripts agree.

A section that talks in another tree's paths marks itself `<!-- map:external -->`
and is left out of the path check until the next heading.

What it cannot check is the prose, and there is one rule it cannot enforce:
**a row here is a clause, not a paragraph.** This file is read before anything is
searched, so its cost is paid by every session; the reasoning behind a row
belongs in the header comment of the file it names, where the code that would be
changed is in front of you. The three inventory tables were 63% of this document
once, restating headers that were already better. When a file's job changes, its
row here is part of that change — as one clause.
